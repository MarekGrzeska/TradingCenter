# TradingCenter

Trading project workspace.

## Spec-driven development

This repo uses [OpenSpec](https://github.com/Fission-AI/OpenSpec). Specs and change proposals live in `openspec/`.

- Propose a change: `/opsx:propose "your idea"`
- CLI: `npx @fission-ai/openspec@latest --help`

## Status

Scaffolding only — stack and structure to be defined.
