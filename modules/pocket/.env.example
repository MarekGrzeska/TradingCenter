# Where the prediction-market archive answers. A relative path goes through Vite's proxy in dev and
# through whatever fronts the app in production; an absolute URL bypasses both.
VITE_POLYMARKET_HTTP=/polymarket-api

# Server-side, so it never reaches the browser: what the dev proxy forwards `/polymarket-api` to.
POLYMARKET_PROXY_TARGET=http://localhost:8070
