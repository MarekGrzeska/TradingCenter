"""The assembly: one process over two surfaces. This package is the only one that imports all three — see
`tests/test_layering.py`, which is where that sentence is enforced rather than merely written."""
