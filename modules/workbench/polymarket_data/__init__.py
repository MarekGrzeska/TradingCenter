"""The prediction-market archive: one module, two surfaces in one process — the REST contract the
terminal reads, and the tool surface at `/mcp`. The same shape as `market-data`, for the same reason."""
