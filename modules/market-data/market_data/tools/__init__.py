"""Narzędzia MCP: the tool surface this module serves at `/mcp`.

The reduction, the ceilings and the sentences about uncertainty — everything a model
needs that a chart does not. A chart wants every candle; a model wants a summary, so
the same archive read comes out reduced rather than whole.
"""
